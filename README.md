# Email-Sender
This script is an Auto Email Sender with CSV built in Python. It reads recipient information (Name, Email, Subject, Message) from a CSV file and sends personalized emails via Gmail’s SMTP server.
